{
  "version": "1.0.0",
  "workspaces": ["backend","app"],
  "scripts": {
    "run": "run-p run:backend run:app",
    "run:backend":"npm run start -w backend",
    "run:app":"npm run start -w app"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
